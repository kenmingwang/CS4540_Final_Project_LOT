<!-- Name: Ken Wang
     UID: u1193853 -->
@model IEnumerable<CS4540_A2.Models.LearningOutcome>

@{
    ViewData["Title"] = "Index";
}

<h1 class="display-4">Index</h1>

<p>
    <a asp-action="CreateLOS">Create New Learning Outcome</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Course.Dept)
                </td>
                <td>
                    <a asp-controller="LearningOutcomes" asp-action="Edit" asp-route-id="@item.LId">Edit</a> 
                    <a asp-controller="LearningOutcomes" asp-action="Delete" asp-route-id="@item.LId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
